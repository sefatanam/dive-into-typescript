import { rawJsonData } from "./rawJsonData";

// convert to formdata from json
// const formData = new FormData();
// formData.append("json", JSON.stringify(rawJsonData));
// console.log(formData);
var formData = new FormData();
console.log("Hello");
